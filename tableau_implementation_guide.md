# Tableau重访高亮地图实现指南

## 📊 方案1：XY散点图 + 路径线（推荐）

### 数据准备

#### 需要的数据字段：
```csv
wave_id,pick_sequence,location_id,x_coord,y_coord,aisle,shelf,slot,sku,pick_time,visit_count
W001,1,A0101,10,50,A01,A01-S01,A0101,SKU001,2025-12-03 09:00:01,1
W001,2,A0105,10,70,A01,A01-S01,A0105,SKU002,2025-12-03 09:00:15,1
W001,3,B0201,30,60,B02,B02-S02,B0201,SKU003,2025-12-03 09:00:45,1
W001,4,A0101,10,50,A01,A01-S01,A0101,SKU004,2025-12-03 09:01:20,2  ← 重访！
```

#### 关键计算字段：

**1. 识别重访位置**
```sql
-- 计算字段: Is_Revisit
IF [visit_count] > 1 THEN "Revisit"
ELSE "Normal"
END
```

**2. 重访严重程度**
```sql
-- 计算字段: Revisit_Severity
IF [visit_count] = 1 THEN "Normal"
ELSEIF [visit_count] = 2 THEN "Low"
ELSEIF [visit_count] = 3 THEN "Medium"
ELSE "High"
END
```

**3. 路径连线**
```sql
-- 计算字段: Path_Point_Type
IF [pick_sequence] = 1 THEN "Start"
ELSEIF [pick_sequence] = MAX([pick_sequence]) THEN "End"
ELSE "Middle"
END
```

---

### Tableau操作步骤

#### Step 1: 创建仓库布局底图

```
1. 新建工作表 "Warehouse Layout"

2. 拖拽字段：
   - Columns: X_Coord
   - Rows: Y_Coord
   - Detail: Location_ID

3. 标记类型: 选择"形状" → 选择方形图标

4. 调整大小: Size设为固定大小（表示货架）

5. 颜色: 设为浅灰色（#E0E0E0）
```

#### Step 2: 添加路径轨迹

```
6. 右键 "Warehouse Layout" → 复制工作表 → 重命名为 "Path Visualization"

7. 添加新图层（双击左上角详细信息按钮）

8. 第二层设置：
   - 标记类型: "线"
   - Path: Pick_Sequence（选择"计算方式"为"维度"）
   - Color: Wave_ID
   - Detail: Wave_ID
   - Size: 固定（2-3像素）

9. 调整线条样式: 右键颜色 → 编辑颜色 → 选择对比色
```

#### Step 3: 高亮重访位置（核心！）

```
10. 添加第三个图层（再次点击"添加标记"）

11. 筛选器: 将 Is_Revisit 拖到筛选器 → 只选择 "Revisit"

12. 标记设置：
    - 类型: 圆形
    - Color: 按 Revisit_Severity 着色
      • Normal: 不显示
      • Low: 黄色 (#FFD700)
      • Medium: 橙色 (#FF8C00)
      • High: 红色 (#FF0000)
    - Size: 按 visit_count 调整大小
    - Label: 显示 visit_count 数字

13. 添加边框效果:
    - Color → 边框 → 黑色
    - 透明度: 80%
```

#### Step 4: 添加交互功能

```
14. 工具提示设置:
    - 点击 Tooltip
    - 编辑内容:
      ━━━━━━━━━━━━━━━━━━━
      📍 Location: <Location_ID>
      🔄 Visit Count: <visit_count> times
      ⚠️ Status: <Is_Revisit>
      📦 SKU: <sku>
      🕐 Time: <pick_time>
      ━━━━━━━━━━━━━━━━━━━

15. 添加筛选器（右侧面板）:
    - Wave_ID: 多选下拉框
    - Aisle: 列表筛选
    - Revisit_Severity: 快速筛选

16. 添加高亮动作:
    - Worksheet → Actions → Add Action → Highlight
    - 设置: 鼠标悬停高亮对应路径
```

---

## 🔥 方案2：热力图（密度地图）

适用于：整体分析哪些区域重访频繁

### Tableau操作：

```
1. 新建工作表 "Heatmap - Revisit Density"

2. 标记类型: 选择 "密度"

3. 拖拽字段：
   - Columns: X_Coord
   - Rows: Y_Coord
   - Color: SUM(visit_count)
   - Detail: Location_ID

4. 调整颜色梯度:
   - 双击颜色
   - 选择调色板: "红-黄-绿"（反向）
     • 绿色: 1次访问（正常）
     • 黄色: 2-3次（需注意）
     • 红色: 4+次（严重）

5. 调整密度强度:
   - Color → 编辑颜色 → 高级
   - 强度滑块: 50-70%
   - 效果: "炫光"

6. 叠加网格线:
   - Format → Lines → Grid Lines
   - 显示Aisle分界线
```

---

## 📈 方案3：路径动画（推荐用于演示）

### Tableau 2020.1+ 支持页面动画

```
1. 基于方案1的工作表

2. 添加页面功能:
   - 将 Pick_Sequence 拖到 "页面" 区域

3. 显示页面控制器:
   - 右上角 → 显示页面控制

4. 设置动画:
   - Format → Animations → On
   - 速度: 慢（2秒）
   - 样式: 线性

5. 播放效果:
   - 点击播放按钮
   - 路径会逐步绘制
   - 重访位置会闪烁高亮
```

---

## 🎯 方案4：仪表板组合（最完整）

### Dashboard布局：

```
┌─────────────────────────────────────────────────────────┐
│  📊 Wave Path Quality Dashboard                         │
├───────────────────┬─────────────────────────────────────┤
│                   │  📈 KPI Cards                       │
│                   ├─────────────────────────────────────┤
│                   │ Aisle    │ Shelf    │ Slot         │
│   🗺️ Map          │ Revisit  │ Revisit  │ Revisit      │
│   (方案1)          │   🔴 5   │   🟡 8   │   🟢 2       │
│                   ├─────────────────────────────────────┤
│   - 底图:货架布局  │  🔍 Filters                         │
│   - 蓝线:路径轨迹  │  Wave: [W001 ▼]                    │
│   - 红圈:重访点    │  Aisle: [All ☑]                    │
│                   │  Show Only Revisits: [ ]            │
│                   ├─────────────────────────────────────┤
│                   │  📋 Revisit Detail Table            │
│                   │  Location │ Count │ Type           │
│                   │  A0101    │  3x   │ Slot           │
│                   │  A01-S01  │  5x   │ Shelf          │
│                   │  Aisle-A  │  2x   │ Aisle          │
└───────────────────┴─────────────────────────────────────┘
```

### 实现步骤：

```
1. 创建Dashboard: "Wave Analysis"

2. 拖入工作表:
   - 左侧大区域: "Path Visualization" (方案1)
   - 右上: KPI卡片（单独创建的工作表）
   - 右中: 筛选器面板
   - 右下: 详情表格

3. 添加交互动作:
   ① 点击地图上的点 → 高亮对应行在表格
   ② 点击表格行 → 地图上闪烁对应位置
   ③ 选择Wave筛选 → 所有图表联动更新

4. 添加参数控制:
   - 创建参数: "Revisit_Threshold"（默认值2）
   - 计算字段:
     IF [visit_count] >= [Revisit_Threshold]
     THEN "Highlight" ELSE "Normal" END
   - 用户可拖动滑块调整阈值

5. 格式美化:
   - Dashboard → Size: 1920x1080（适配大屏）
   - 背景色: 深色主题 (#1E1E1E)
   - 字体: 思源黑体 或 Arial
```

---

## 🎨 进阶技巧

### 1. 自定义形状（更形象的货架）

```
步骤：
1. 准备货架SVG图标
2. 我的文档/My Tableau Repository/Shapes/
3. 创建文件夹 "Warehouse"
4. 放入图标文件
5. Tableau → 重新加载形状
6. 标记 → 形状 → 选择自定义形状
```

### 2. 参数化配色

```
创建参数: Color_Scheme
值: "Standard" | "Colorblind Safe" | "High Contrast"

计算字段: Dynamic_Color
CASE [Color_Scheme]
  WHEN "Standard" THEN
    IF [visit_count] > 3 THEN "#FF0000"
    ELSE "#FFD700" END
  WHEN "Colorblind Safe" THEN
    IF [visit_count] > 3 THEN "#0173B2"
    ELSE "#DE8F05" END
  ELSE "#FFFFFF"
END
```

### 3. 导出高清图片

```
Dashboard → Export Image
- 分辨率: 300 DPI
- 格式: PNG（带透明背景）
- 用于: PPT/报告
```

---

## 📊 数据预处理（重要！）

### 在导入Tableau前计算重访次数：

#### Python脚本（自动计算）：

```python
import pandas as pd

# 读取拣货数据
df = pd.read_excel("picking_data.xlsx")

# 按Wave分组计算每个位置的访问次数
df_revisit = df.groupby(['wave_id', 'location_id']).agg(
    visit_count=('location_id', 'size'),
    first_visit_seq=('pick_sequence', 'min'),
    last_visit_seq=('pick_sequence', 'max'),
    x_coord=('x_coord', 'first'),
    y_coord=('y_coord', 'first'),
    aisle=('aisle', 'first')
).reset_index()

# 标记重访
df_revisit['is_revisit'] = df_revisit['visit_count'] > 1

# 计算重访间隔
df_revisit['revisit_gap'] = df_revisit['last_visit_seq'] - df_revisit['first_visit_seq']

# 导出给Tableau
df_revisit.to_csv("tableau_revisit_data.csv", index=False)
```

#### SQL版本（如果数据在数据库）：

```sql
WITH revisit_calc AS (
  SELECT
    wave_id,
    location_id,
    COUNT(*) as visit_count,
    MIN(pick_sequence) as first_visit_seq,
    MAX(pick_sequence) as last_visit_seq,
    MAX(x_coord) as x_coord,
    MAX(y_coord) as y_coord,
    MAX(aisle) as aisle,
    MAX(shelf) as shelf
  FROM picking_detail
  GROUP BY wave_id, location_id
)
SELECT
  *,
  CASE
    WHEN visit_count > 1 THEN 'Revisit'
    ELSE 'Normal'
  END as is_revisit,
  last_visit_seq - first_visit_seq as revisit_gap
FROM revisit_calc
ORDER BY wave_id, visit_count DESC;
```

---

## 🚀 Tableau Public示例（可参考）

如果你想看实际效果，可以搜索这些Tableau Public案例：

```
搜索关键词：
1. "warehouse heatmap"
2. "route visualization"
3. "path analysis"
4. "logistics tracking"
```

---

## ⚡ 快速启动清单

### 今天就能做：

- [ ] 导出你的拣货数据（包含XY坐标）
- [ ] 用Excel/Python计算visit_count
- [ ] 导入Tableau，创建第一个散点图
- [ ] 按visit_count着色

### 明天可以做：

- [ ] 添加路径连线
- [ ] 配置高亮效果
- [ ] 添加筛选器

### 本周完成：

- [ ] 创建完整Dashboard
- [ ] 配置交互动作
- [ ] 准备演示版本

---

## 💡 需要我帮忙吗？

我可以：
1. ✅ 写Python脚本自动生成Tableau数据源
2. ✅ 提供Tableau工作簿模板（.twb文件）
3. ✅ 帮你设计Dashboard布局
4. ✅ 生成测试数据（如果你想先试试效果）

**需要哪一项？告诉我！** 🎨
